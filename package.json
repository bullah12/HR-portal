{
  "name": "hr-portal",
  "version": "0.1.0",
  "private": true,
  "description": "HR Portal — recruitment-to-onboarding platform (Phase 1: database schema + seed)",
  "scripts": {
    "db:push": "prisma db push",
    "db:seed": "ts-node prisma/seed.ts"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
